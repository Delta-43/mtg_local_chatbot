I want to create an AI chatbot that can act as a judge for Magic The Gathering games. The idea is to create a RAG assisted chabot using a local llm with ollama, to get the answers for basic rules and when it involves a card have sub agent that connects to scryfall.com via an API to retrieve the specific card details and then merge the contexts to give the correct answers. For the basic rules it uses a parser to convert the official rules pdf into a semantic database using a parser which I have already made in the pdf_parser folder. It needs to be modified a bit to be able to download the latest version if it has been updated and then update the local file and re iniate the database. I want to include Chroma for the data infrastructure and Langchain for the different agents to connect to each other. For more extended context I would like to use a free API for Reddit or scrape MTGsalvation forums for these queries. I'm going to run this locally on a old computer with a i5-4590 and AMD R7 250E, so I want to use a low weight LLM. Could you help me plan this. I already have an example of a similar project in the example_pipeline folder as available on github at https://github.com/ThomasJanssen-tech/Local-RAG-with-Ollama. I have decided that the Qwen3.5:0.8b model would be appropiate for this hardware along with the mxbai-embed-large model for embeddings. I wish to run this in a docker container where the chatbot is accessible via an API to be used later for creating integrations in a webapp or Telegram/Discord bot. Could you help me build this?

The plan would be like this -
Task 0: "Setup the required tools for the project. This maybe modified using a configuration file. Settings may include model being used for llm and the embeddings and so on. Use the example for inspiration"
Task 1: "Update pdf_parser/MTG_Rules_PDF_Parser_v0.4.py to automatically download the latest rules PDF from the official MTG website if it's newer than the local copy."
Task 2: "Create a new script in local_llm/ that reads MagicCompRule_parsed_hierarchical.json, formats it into Langchain Documents, and ingests it into a local ChromaDB using Ollama embeddings."
Task 3: "Create a script in scryfall_agent/ that defines a Langchain Tool for querying the Scryfall API."
Task 4: "Create the main chatbot.py script that uses Langchain to route queries between the ChromaDB retriever and the Scryfall tool, using an Ollama LLM to generate the final response."

Don't touch the existing files in pdf_parser_old, recreate it in the pdf_parser folder. Check your work after steps to ensure it was completed properly